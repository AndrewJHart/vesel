{{!-- No reason to embed an actual header view here since no real functionality is required --}}
<header class="bar bar-nav">
  	{{#link "" expand-tokens=true class="icon icon-left-nav pull-left"}}{{/link}}
  	<h1 class="title">Details</h1>
</header>

<div class="content">
  <div class="{{category.name}}__bg {{category.name}}-d__bg">
    <div class="detail-cat__icon">
      <span class="{{category.name}}"></span>
    </div>
    <div class="detail-cat__subject">
        <h4>{{subject}}</h4>
    </div>
  </div>
    
  <ul class="table-view table-view_detail__push">
      {{!-- map container --}}
      <div id="single-map" class="map" style="display: {{mapVisibility}}">
      </div>

      {{!-- notification detail text --}}
      <li class="table-view-cell table-view_detail__push">
          <p>{{information}}</p>
      </li>
    </ul>
</div>


{{!-- No reason to create an extra view for the footer either. This allows us to handle events directly --}}
<nav class="bar bar-tab">
  <a class="tab-item active toggle-share">
    <span class="icon icon-share"></span>
    <span class="tab-label">Share This</span>
  </a>
</nav>